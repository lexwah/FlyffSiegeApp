import React from 'react';
import './style.css';

const Header = (): React.ReactElement => {
  const s = '';

  return (
    <div className="header">
      {s}
    </div>
  );
};

export default Header;
